# Pages

This folder contains the application's route pages (e.g., login, dashboard, profile).

Each subfolder here represents a Next.js App Router page route.

**Example structure:**
```
Pages/
├── login/
│   └── page.tsx
├── dashboard/
│   └── page.tsx
└── profile/
    └── page.tsx
```
